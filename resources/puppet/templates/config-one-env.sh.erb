#!/bin/bash

#echo " === Change Password ==== "
#su -l oneadmin -c 'oneuser passwd oneadmin <%= @oneadmin_pw %>'
#echo 'oneadmin:<%= @oneadmin_pw %>' > /var/lib/one/.one/one_auth

#su -l oneadmin -c 'onehost create master -i kvm -v kvm -n ebtable'
#su -l oneadmin -c 'onehost create slave-1 -i kvm -v kvm -n ebtable'
#su -l oneadmin -c 'onehost create slave-2 -i kvm -v kvm -n ebtable'

echo " ========== Restart OpenNebula Service ==== "
service opennebula restart
service opennebula-sunstone restart

#echo " ========== Download Test VM Image ======== "
#wget "https://onedrive.live.com/download?resid=28f8f701dc29e4b9%2110219" -O /usr/local/src/Ubuntu-12.04.qcow2.gz

echo " === Build Test Network/Image/Template ==== "
su -l oneadmin << EOF
onevnet create /vagrant/resources/puppet/files/one-test-network.tmpl

oneimage create \
--name "Ubuntu-12.04" \
--path "/usr/local/src/Ubuntu-12.04.qcow2.gz" \
--driver qcow2 \
--datastore default

#while ! oneimage list | grep 'Ubuntu-12.04' | grep -q rdy
#do
#	sleep 60
#done

onetemplate create \
--name "Ubuntu-12.04" \
--cpu 1 \
--vcpu 1 \
--memory 512 \
--arch x86_64 \
--disk "Ubuntu-12.04" \
--nic "Test Network" \
--context DNS=8.8.8.8 \
--context GATEWAY=192.168.33.2 \
--ssh /var/lib/one/.ssh/id_rsa.pub \
--vnc
EOF

echo
echo " ========================================== "
echo " ============= Completed !!! ============== "
echo " ========================================== "
echo

exit 0
